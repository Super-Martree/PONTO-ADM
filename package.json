{
  "name": "martree-ponto",
  "private": true,
  "version": "1.0.0",
  "description": "Orquestrador local do Martree Ponto.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "install:all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "build": "npm.cmd run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
